# GitHub PM — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add an optional GitHub PM pillar to aia-harness that auto-detects GitHub remotes and distributes a first-party `github-pm` skill, 10 `/pm:*` commands, issue/PR templates, 4 GitHub Actions workflows, and vendored `github-issues`/`github-project` skills into target projects.

**Architecture:** New detector (`lib/detect/github-pm.mjs`) → new catalog (`lib/data/github-pm-catalog.mjs`) → wired into `buildPlan` as `category:'github-pm', defaultSelected:false`. Shell scripts in `templates/skills/github-pm/scripts/` back the merge-gate and worktree-safety commands. Vendored skills fetched by new `scripts/sync-*.mjs` via the same pattern as ECC/ag-kit.

**Tech Stack:** Node ≥18 ESM (.mjs), JSDoc+checkJs, node:test, shell (bash), YAML (GitHub Actions), Markdown (SKILL.md, commands), JSON (catalogs/config)

## Global Constraints

- All source files `.mjs` ESM + JSDoc — no TypeScript, no build step
- `lib/` pure and tested — no IO inside detectors, generators, catalogs
- `templates/` excluded from lint/typecheck
- `defaultSelected: false` for all github-pm artifacts — opt-in only
- `category: 'github-pm'` on every artifact added in buildPlan
- `contextCost: 0` on all github-pm artifacts (lazy/path-scoped)
- Shell scripts must be executable (`chmod +x`) and fail-open on missing tools
- GitHub Actions workflows fail-open when `PROJECTS_PAT` secret absent
- `pm-config.json` never overwritten by apply (apply's safe-by-default handles this)
- Provenance stamp on every vendored file; licenses preserved
- `templates/github-pm-ext/MANIFEST.json` tracks vendored commits
- MCP github server already in `mcp-catalog.mjs` — do NOT duplicate
- Run `npm test` (typecheck + lint + unit) after every commit

---

## File Map

### Created — Engine

| File | Responsibility |
| --- | --- |
| `lib/detect/github-pm.mjs` | Pure detector: reads `profile.vcs` + file list → `GitHubPMInfo` |
| `lib/data/github-pm-catalog.mjs` | Returns `GitHubPMArtifactDef[]` for `buildPlan` |
| `lib/github-pm/transform.mjs` | Pure transform for vendored SKILL.md files (frontmatter + provenance stamp) |

### Modified — Engine

| File | Change |
| --- | --- |
| `lib/profile.mjs` | Add `GitHubPMInfo` typedef; add `remoteUrl` to `VcsInfo`; add `githubPM` to `ProjectProfile` |
| `lib/detect/vcs.mjs` | Parse `.git/config` to populate `remoteUrl` |
| `lib/detect/index.mjs` | Import + call `detectGitHubPM`; initialize stub in profile |
| `lib/data/asset-catalog.mjs` | Re-export `selectGitHubPMAssets` from github-pm-catalog |
| `lib/plan.mjs` | Add github-pm block after existing catalog blocks |
| `package.json` | Add `sync:github-issues` and `sync:github-project` scripts |

### Created — Scripts (vendoring)

| File | Responsibility |
| --- | --- |
| `scripts/github-issues-source.json` | Pins repo + commit for github-issues |
| `scripts/github-project-source.json` | Pins repo + commit for github-project |
| `scripts/sync-github-issues.mjs` | Fetch + transform + stamp github-issues |
| `scripts/sync-github-project.mjs` | Fetch + transform + stamp github-project |

### Created — Templates

```
templates/
├── skills/github-pm/
│   ├── SKILL.md
│   ├── references/
│   │   ├── 01-criar-issue.md
│   │   ├── 02-trabalhar-issue.md
│   │   ├── 03-fechar-issue.md
│   │   ├── 04-backlog.md
│   │   └── pm-config-schema.md
│   └── scripts/
│       ├── check-pr-status.sh
│       └── worktree-safety-check.sh
├── commands/pm/
│   ├── issue-new.md
│   ├── issue-work.md
│   ├── issue-close.md
│   ├── backlog.md
│   ├── setup-project.md
│   ├── worktree-new.md
│   ├── commit-push-pr.md
│   ├── pr-merge.md
│   ├── worktree-remove.md
│   └── code-review-pr.md
├── github/
│   ├── ISSUE_TEMPLATE/bug.yml
│   ├── ISSUE_TEMPLATE/feature.yml
│   ├── ISSUE_TEMPLATE/task.yml
│   ├── PULL_REQUEST_TEMPLATE.md
│   ├── workflows/issue-to-project.yml
│   ├── workflows/commit-to-progress.yml
│   ├── workflows/pr-to-review.yml
│   ├── workflows/auto-close-issue.yml
│   └── pm-config.json.template
└── github-pm-ext/
    ├── MANIFEST.json
    ├── github-issues/   (populated by sync:github-issues)
    └── github-project/  (populated by sync:github-project)
```

### Created — Plugin Commands

| File | Responsibility |
| --- | --- |
| `commands/add-github-pm.md` | `/add-github-pm` — activate GitHub PM in existing harness |

### Modified — Plugin Commands

| File | Change |
| --- | --- |
| `commands/init.md` | Add github-pm to consent gate AskUserQuestion |
| `commands/patch.md` | Add `github-pm` row to category table |
| `commands/scan.md` | Report `githubPM` section in diagnosis |
| `commands/doctor.md` | Audit github-pm artifacts |

### Created — Tests + Fixtures

| File | Responsibility |
| --- | --- |
| `tests/detect-github-pm.test.mjs` | Unit: detector all branches |
| `tests/catalog-github-pm.test.mjs` | Unit: selectGitHubPMAssets shape |
| `tests/plan-github-pm.test.mjs` | Integration: plan includes category when detected |
| `tests/fixtures/github-project/` | git repo with github.com remote + .github/ |
| `tests/fixtures/no-github-project/` | git repo with non-github remote |

---

## Task 1: VcsInfo.remoteUrl + GitHubPMInfo types + detector (TDD)

**Files:**
- Create: `tests/detect-github-pm.test.mjs`
- Create: `tests/fixtures/github-project/.git/HEAD`
- Create: `tests/fixtures/github-project/.git/config`
- Create: `tests/fixtures/github-project/.github/ISSUE_TEMPLATE/bug.yml`
- Create: `tests/fixtures/github-project/.github/workflows/ci.yml`
- Create: `tests/fixtures/no-github-project/.git/HEAD`
- Create: `tests/fixtures/no-github-project/.git/config`
- Modify: `lib/profile.mjs`
- Modify: `lib/detect/vcs.mjs`
- Create: `lib/detect/github-pm.mjs`
- Modify: `lib/detect/index.mjs`

**Interfaces:**
- Produces: `detectGitHubPM(profile, files) → GitHubPMInfo` — consumed by Task 8
- Produces: `profile.githubPM: GitHubPMInfo` — consumed by Task 8 catalog
- Produces: `profile.vcs.remoteUrl: string|null` — consumed by detector

- [ ] **Step 1: Write failing tests**

```js
// tests/detect-github-pm.test.mjs
import { test } from "node:test";
import assert from "node:assert/strict";
import path from "node:path";
import { fileURLToPath } from "node:url";
import { detectGitHubPM } from "../lib/detect/github-pm.mjs";
import { scanProject } from "../lib/detect/index.mjs";

const FIX = path.join(path.dirname(fileURLToPath(import.meta.url)), "fixtures");

function files(rels) {
  return rels.map(r => ({ rel: r, base: r.split("/").at(-1) }));
}

test("detected: true when remote contains github.com", () => {
  const p = { vcs: { isGit: true, remoteUrl: "https://github.com/acme/repo.git", defaultBranch: "main", worktreeReady: true } };
  assert.equal(detectGitHubPM(p, []).detected, true);
});

test("detected: false when no github.com in remote", () => {
  const p = { vcs: { isGit: true, remoteUrl: "https://gitlab.com/acme/repo.git", defaultBranch: "main", worktreeReady: true } };
  assert.equal(detectGitHubPM(p, []).detected, false);
});

test("detected: false when not a git repo", () => {
  const p = { vcs: { isGit: false, remoteUrl: null, defaultBranch: null, worktreeReady: false } };
  assert.equal(detectGitHubPM(p, []).detected, false);
});

test("detected: false when remoteUrl is null", () => {
  const p = { vcs: { isGit: true, remoteUrl: null, defaultBranch: "main", worktreeReady: true } };
  assert.equal(detectGitHubPM(p, []).detected, false);
});

test("hasIssueTemplates: true when .github/ISSUE_TEMPLATE in rel path", () => {
  const p = { vcs: { isGit: true, remoteUrl: "https://github.com/a/b.git", defaultBranch: "main", worktreeReady: true } };
  const r = detectGitHubPM(p, files([".github/ISSUE_TEMPLATE/bug.yml"]));
  assert.equal(r.hasIssueTemplates, true);
});

test("hasWorkflows: true when .github/workflows in rel path", () => {
  const p = { vcs: { isGit: true, remoteUrl: "https://github.com/a/b.git", defaultBranch: "main", worktreeReady: true } };
  const r = detectGitHubPM(p, files([".github/workflows/ci.yml"]));
  assert.equal(r.hasWorkflows, true);
});

test("hasPmConfig: true when .claude/pm-config.json ends rel path", () => {
  const p = { vcs: { isGit: true, remoteUrl: "https://github.com/a/b.git", defaultBranch: "main", worktreeReady: true } };
  const r = detectGitHubPM(p, files([".claude/pm-config.json"]));
  assert.equal(r.hasPmConfig, true);
});

test("GitHubPMInfo shape has all required fields", () => {
  const p = { vcs: { isGit: false, remoteUrl: null, defaultBranch: null, worktreeReady: false } };
  const r = detectGitHubPM(p, []);
  assert.ok("detected" in r);
  assert.ok("hasIssueTemplates" in r);
  assert.ok("hasWorkflows" in r);
  assert.ok("hasPmConfig" in r);
});

test("integration: github-project fixture detected=true with .github/ signals", () => {
  const p = scanProject(path.join(FIX, "github-project"));
  assert.equal(p.githubPM.detected, true);
  assert.equal(p.githubPM.hasIssueTemplates, true);
  assert.equal(p.githubPM.hasWorkflows, true);
});

test("integration: no-github-project fixture detected=false", () => {
  const p = scanProject(path.join(FIX, "no-github-project"));
  assert.equal(p.githubPM.detected, false);
});
```

- [ ] **Step 2: Run tests — expect FAIL (module not found)**

```bash
node --test tests/detect-github-pm.test.mjs
```

Expected: `Error: Cannot find module '../lib/detect/github-pm.mjs'`

- [ ] **Step 3: Create fixtures**

```bash
mkdir -p tests/fixtures/github-project/.git
mkdir -p tests/fixtures/github-project/.github/ISSUE_TEMPLATE
mkdir -p tests/fixtures/github-project/.github/workflows
mkdir -p tests/fixtures/no-github-project/.git
```

`tests/fixtures/github-project/.git/HEAD`:
```
ref: refs/heads/main
```

`tests/fixtures/github-project/.git/config`:
```
[core]
	repositoryformatversion = 0
[remote "origin"]
	url = https://github.com/acme/test-repo.git
	fetch = +refs/heads/*:refs/remotes/origin/*
```

`tests/fixtures/github-project/.github/ISSUE_TEMPLATE/bug.yml`:
```yaml
name: Bug Report
about: Report a bug
```

`tests/fixtures/github-project/.github/workflows/ci.yml`:
```yaml
name: CI
on: [push]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
```

`tests/fixtures/no-github-project/.git/HEAD`:
```
ref: refs/heads/main
```

`tests/fixtures/no-github-project/.git/config`:
```
[core]
	repositoryformatversion = 0
[remote "origin"]
	url = https://gitlab.com/acme/other-repo.git
	fetch = +refs/heads/*:refs/remotes/origin/*
```

- [ ] **Step 4: Add typedef to `lib/profile.mjs`**

Add after the `LargeFilesInfo` typedef and before `ProjectProfile`:

```js
/**
 * @typedef {Object} GitHubPMInfo
 * @property {boolean} detected          Remote contains github.com and isGit=true.
 * @property {boolean} hasIssueTemplates .github/ISSUE_TEMPLATE/ path found in file list.
 * @property {boolean} hasWorkflows      .github/workflows/ path found in file list.
 * @property {boolean} hasPmConfig       .claude/pm-config.json found in file list.
 */
```

Add `remoteUrl` to `VcsInfo` typedef (replace existing):

```js
/**
 * @typedef {Object} VcsInfo
 * @property {boolean} isGit
 * @property {boolean} worktreeReady
 * @property {string|null} defaultBranch
 * @property {string|null} remoteUrl     URL of the first remote found in .git/config, or null.
 */
```

Add `githubPM` to `ProjectProfile` typedef (after `largeFiles` line):

```js
 * @property {GitHubPMInfo} githubPM     GitHub PM detection results.
```

- [ ] **Step 5: Update `lib/detect/vcs.mjs` to populate `remoteUrl`**

Current file reads `.git/HEAD`. Add `.git/config` parsing:

```js
/**
 * Git / version-control detection.
 * @module detect/vcs
 */
import path from "node:path";
import { exists, readText } from "../util/fs.mjs";

/**
 * @param {string} root
 * @returns {import('../profile.mjs').VcsInfo}
 */
export function detectVcs(root) {
  const isGit = exists(path.join(root, ".git"));
  /** @type {string|null} */
  let defaultBranch = null;
  /** @type {string|null} */
  let remoteUrl = null;
  const head = readText(path.join(root, ".git", "HEAD"));
  if (head) {
    const m = head.match(/ref:\s*refs\/heads\/(.+)\s*$/m);
    if (m) defaultBranch = m[1].trim();
  }
  const config = readText(path.join(root, ".git", "config"));
  if (config) {
    const m = config.match(/url\s*=\s*(.+)/);
    if (m) remoteUrl = m[1].trim();
  }
  return { isGit, worktreeReady: isGit, defaultBranch, remoteUrl };
}
```

- [ ] **Step 6: Create `lib/detect/github-pm.mjs`**

```js
/**
 * GitHub PM detection: checks if the project uses GitHub as VCS remote.
 * @module detect/github-pm
 */

/**
 * @param {import('../profile.mjs').ProjectProfile} profile
 * @param {{ rel: string, base: string }[]} files
 * @returns {import('../profile.mjs').GitHubPMInfo}
 */
export function detectGitHubPM(profile, files) {
  const remote = profile.vcs.remoteUrl ?? '';
  const detected = profile.vcs.isGit && remote.includes('github.com');
  return {
    detected,
    hasIssueTemplates: files.some(f => f.rel.includes('.github/ISSUE_TEMPLATE')),
    hasWorkflows:      files.some(f => f.rel.includes('.github/workflows')),
    hasPmConfig:       files.some(f => f.rel.endsWith('.claude/pm-config.json')),
  };
}
```

- [ ] **Step 7: Integrate detector into `lib/detect/index.mjs`**

Add import after existing detector imports:
```js
import { detectGitHubPM } from "./github-pm.mjs";
```

Add stub to the `profile` object (inside the object literal, after `truncated`):
```js
    githubPM: /** @type {import('../profile.mjs').GitHubPMInfo} */ ({
      detected: false,
      hasIssueTemplates: false,
      hasWorkflows: false,
      hasPmConfig: false,
    }),
```

Add call after `profile.largeFiles = detectLargeFiles(abs, files);`:
```js
  profile.githubPM = detectGitHubPM(profile, files);
```

- [ ] **Step 8: Run tests — expect PASS**

```bash
node --test tests/detect-github-pm.test.mjs
```

Expected: all 10 tests pass

- [ ] **Step 9: Run full test suite**

```bash
npm test
```

Expected: typecheck + lint + all unit tests pass

- [ ] **Step 10: Commit**

```bash
git add lib/profile.mjs lib/detect/vcs.mjs lib/detect/github-pm.mjs lib/detect/index.mjs \
        tests/detect-github-pm.test.mjs \
        tests/fixtures/github-project/.git/HEAD \
        tests/fixtures/github-project/.git/config \
        tests/fixtures/github-project/.github/ISSUE_TEMPLATE/bug.yml \
        tests/fixtures/github-project/.github/workflows/ci.yml \
        tests/fixtures/no-github-project/.git/HEAD \
        tests/fixtures/no-github-project/.git/config
git commit -m "feat: add GitHubPMInfo detector + VcsInfo.remoteUrl"
```

---

## Task 2: Shell scripts

**Files:**
- Create: `templates/skills/github-pm/scripts/check-pr-status.sh`
- Create: `templates/skills/github-pm/scripts/worktree-safety-check.sh`

**Interfaces:**
- Consumed by: `/pm:pr-merge` command (Task 4) and `/pm:worktree-remove` command (Task 4)

- [ ] **Step 1: Create directory**

```bash
mkdir -p templates/skills/github-pm/scripts
```

- [ ] **Step 2: Write `check-pr-status.sh`**

```bash
# templates/skills/github-pm/scripts/check-pr-status.sh
#!/usr/bin/env bash
# Validate PR CI status and review approval before merge.
# Usage: check-pr-status.sh <PR_NUMBER> <OWNER/REPO>
# Exit 0: all checks passed + review approved (or no branch protection)
# Exit 1: one or more checks failing
# Exit 2: checks still pending
# Exit 3: PR not found, invalid, or no checks registered
# Exit 4: checks OK but no approved review

set -euo pipefail

PR_NUMBER="${1:-}"
OWNER_REPO="${2:-}"

if [[ -z "$PR_NUMBER" || -z "$OWNER_REPO" ]]; then
  echo "Usage: $0 <PR_NUMBER> <OWNER/REPO>" >&2
  exit 3
fi

# Fetch PR state
PR_JSON=$(gh pr view "$PR_NUMBER" --repo "$OWNER_REPO" \
  --json state,isDraft,mergeStateStatus,statusCheckRollup,reviewDecision 2>/dev/null) || {
  echo "PR #$PR_NUMBER not found in $OWNER_REPO" >&2
  exit 3
}

STATE=$(echo "$PR_JSON" | python3 -c "import sys,json; d=json.load(sys.stdin); print(d['state'])")
IS_DRAFT=$(echo "$PR_JSON" | python3 -c "import sys,json; d=json.load(sys.stdin); print(str(d['isDraft']).lower())")

if [[ "$STATE" != "OPEN" ]]; then
  echo "PR #$PR_NUMBER is $STATE (not OPEN)" >&2
  exit 3
fi

if [[ "$IS_DRAFT" == "true" ]]; then
  echo "PR #$PR_NUMBER is a draft" >&2
  exit 3
fi

# Check CI rollup
ROLLUP=$(echo "$PR_JSON" | python3 -c "
import sys, json
d = json.load(sys.stdin)
checks = d.get('statusCheckRollup', [])
if not checks:
    print('NONE')
    sys.exit(0)
states = [c.get('conclusion') or c.get('state') or 'PENDING' for c in checks]
if any(s in ('FAILURE', 'ERROR', 'TIMED_OUT', 'CANCELLED') for s in states):
    print('FAILING')
elif any(s in ('PENDING', 'QUEUED', 'IN_PROGRESS', 'WAITING', 'REQUESTED', 'EXPECTED', None) for s in states):
    print('PENDING')
else:
    print('SUCCESS')
")

case "$ROLLUP" in
  NONE)
    # No checks registered — fall through to review check
    ;;
  FAILING)
    echo "CI checks are failing for PR #$PR_NUMBER" >&2
    gh pr checks "$PR_NUMBER" --repo "$OWNER_REPO" 2>/dev/null || true
    exit 1
    ;;
  PENDING)
    echo "CI checks still pending for PR #$PR_NUMBER" >&2
    exit 2
    ;;
  SUCCESS)
    ;;
esac

# Check review decision
REVIEW=$(echo "$PR_JSON" | python3 -c "
import sys, json
d = json.load(sys.stdin)
print(d.get('reviewDecision') or 'NONE')
")

if [[ "$REVIEW" == "APPROVED" || "$REVIEW" == "NONE" ]]; then
  echo "PR #$PR_NUMBER: all checks passed" >&2
  exit 0
else
  echo "PR #$PR_NUMBER: checks OK but review not approved (status: $REVIEW)" >&2
  exit 4
fi
```

```bash
chmod +x templates/skills/github-pm/scripts/check-pr-status.sh
```

- [ ] **Step 3: Write `worktree-safety-check.sh`**

```bash
# templates/skills/github-pm/scripts/worktree-safety-check.sh
#!/usr/bin/env bash
# Validate a worktree is safe to remove.
# Usage: worktree-safety-check.sh <WORKTREE_TARGET> <OWNER/REPO>
# WORKTREE_TARGET: branch name, issue number, worktree path, or empty (current)
# Exit 0: safe to remove; prints RESULT_WT_PATH and RESULT_WT_BRANCH on stdout
# Exit 1: blocked (prints checklist with ✅/❌)
# Exit 2: worktree not found

set -euo pipefail

TARGET="${1:-}"
OWNER_REPO="${2:-}"

# Resolve worktree path and branch
WT_JSON=$(git worktree list --porcelain 2>/dev/null) || { echo "Not a git repo" >&2; exit 2; }

resolve_worktree() {
  local target="$1"
  local path="" branch=""

  while IFS= read -r line; do
    if [[ "$line" == worktree* ]]; then path="${line#worktree }"; fi
    if [[ "$line" == branch* ]]; then branch="${line#branch refs/heads/}"; fi
    if [[ -z "$line" && -n "$path" ]]; then
      local match=false
      # Match by path, branch name, or issue number in branch
      [[ "$path" == "$target" ]] && match=true
      [[ "$branch" == "$target" ]] && match=true
      [[ -n "$target" ]] && [[ "$branch" =~ /$target- || "$branch" =~ /$target$ ]] && match=true
      # Skip the main worktree (no .git file, has .git dir)
      if [[ "$match" == "true" && "$path" != "$(git rev-parse --show-toplevel 2>/dev/null)" ]]; then
        echo "$path|$branch"
        return 0
      fi
      path=""; branch=""
    fi
  done <<< "$WT_JSON"
  return 1
}

if [[ -z "$TARGET" ]]; then
  # Use current directory as worktree
  CURRENT=$(pwd)
  MAIN=$(git rev-parse --show-toplevel 2>/dev/null || echo "")
  if [[ "$CURRENT" == "$MAIN" ]]; then
    echo "Already in main worktree — nothing to remove" >&2
    exit 2
  fi
  WT_PATH="$CURRENT"
  WT_BRANCH=$(git branch --show-current 2>/dev/null || echo "")
else
  RESOLVED=$(resolve_worktree "$TARGET") || { echo "Worktree not found for: $TARGET" >&2; exit 2; }
  WT_PATH="${RESOLVED%%|*}"
  WT_BRANCH="${RESOLVED##*|}"
fi

if [[ -z "$WT_PATH" || -z "$WT_BRANCH" ]]; then
  echo "Could not resolve worktree for: $TARGET" >&2
  exit 2
fi

BLOCKED=false

# 1. Working tree clean
if git -C "$WT_PATH" status --porcelain 2>/dev/null | grep -q .; then
  echo "❌ Uncommitted changes in $WT_PATH" >&2
  BLOCKED=true
else
  echo "✅ Working tree clean" >&2
fi

# 2. Nothing committed without push
UNPUSHED=$(git -C "$WT_PATH" log "origin/$WT_BRANCH..$WT_BRANCH" --oneline 2>/dev/null | wc -l | tr -d ' ')
if [[ "$UNPUSHED" -gt 0 ]]; then
  echo "❌ $UNPUSHED commit(s) not pushed to origin/$WT_BRANCH" >&2
  BLOCKED=true
else
  echo "✅ All commits pushed" >&2
fi

# 3. Branch has a PR
if [[ -n "$OWNER_REPO" ]]; then
  PR_NUMBER=$(gh pr list --repo "$OWNER_REPO" --head "$WT_BRANCH" --json number --jq '.[0].number' 2>/dev/null || echo "")
  if [[ -z "$PR_NUMBER" ]]; then
    echo "❌ No PR found for branch $WT_BRANCH" >&2
    BLOCKED=true
  else
    echo "✅ PR #$PR_NUMBER found" >&2

    # 4. CI not failing
    bash "$(dirname "$0")/check-pr-status.sh" "$PR_NUMBER" "$OWNER_REPO" >/dev/null 2>/dev/null
    CI_EXIT=$?
    if [[ $CI_EXIT -eq 1 ]]; then
      echo "❌ CI checks failing on PR #$PR_NUMBER" >&2
      BLOCKED=true
    elif [[ $CI_EXIT -eq 2 ]]; then
      echo "❌ CI checks still pending on PR #$PR_NUMBER" >&2
      BLOCKED=true
    else
      echo "✅ CI checks OK" >&2
    fi

    # 5. PR merged
    PR_STATE=$(gh pr view "$PR_NUMBER" --repo "$OWNER_REPO" --json state --jq '.state' 2>/dev/null || echo "UNKNOWN")
    if [[ "$PR_STATE" != "MERGED" ]]; then
      echo "❌ PR #$PR_NUMBER not merged (state: $PR_STATE)" >&2
      BLOCKED=true
    else
      echo "✅ PR #$PR_NUMBER merged" >&2
    fi
  fi
fi

if [[ "$BLOCKED" == "true" ]]; then
  exit 1
fi

echo "RESULT_WT_PATH=$WT_PATH"
echo "RESULT_WT_BRANCH=$WT_BRANCH"
exit 0
```

```bash
chmod +x templates/skills/github-pm/scripts/worktree-safety-check.sh
```

- [ ] **Step 4: Commit**

```bash
git add templates/skills/github-pm/scripts/
git commit -m "feat: add check-pr-status and worktree-safety-check shell scripts"
```

---

## Task 3: First-party skill + references

**Files:**
- Create: `templates/skills/github-pm/SKILL.md`
- Create: `templates/skills/github-pm/references/01-criar-issue.md`
- Create: `templates/skills/github-pm/references/02-trabalhar-issue.md`
- Create: `templates/skills/github-pm/references/03-fechar-issue.md`
- Create: `templates/skills/github-pm/references/04-backlog.md`
- Create: `templates/skills/github-pm/references/pm-config-schema.md`

- [ ] **Step 1: Create `templates/skills/github-pm/SKILL.md`**

```markdown
---
name: github-pm
description: >
  This skill should be used when the user mentions tickets, issues, backlog,
  PR, pull request, worktree, sprint, or any development project management
  activity. Also activate when the user says "create issue", "work on #N",
  "close ticket", "open PR", "merge PR", "view backlog", "create branch for
  issue", or when code was modified without a linked issue.
---

# GitHub PM — ciclo de vida de issues e Projects v2

Você é o orquestrador de PM para projetos com GitHub. Seu papel é garantir
que todo trabalho de código esteja vinculado a uma issue, que o status do
Projects v2 reflita o estado real, e que nenhum código vá para main sem CI e review.

## Pré-condição obrigatória

Antes de qualquer operação com Projects v2, leia `.claude/pm-config.json`:

```bash
cat .claude/pm-config.json 2>/dev/null || echo "NOT_FOUND"
```

Se retornar `NOT_FOUND`, instrua o usuário a rodar `/pm:setup-project` e pare.
Nunca tente inferir project_id ou status_field_id — use apenas os IDs do arquivo.

## Ciclo de vida

```
Backlog → In Progress → In Review → Done
```

Nunca pule um estado. Nunca regrida um status (ex.: In Review permanece In Review
se um novo commit for feito enquanto o PR está aberto).

## Mapa de delegação

Para CRUD de issues (criar, listar, editar, fechar, sub-issues, campos, Projects v2):
→ Use a skill `github-issues`. Ela provê ferramentas MCP (`mcp__github__projects_write`,
  `mcp__github__search_issues`, etc.) que já cobrem tudo. Não reinvente esse CRUD.

Para troubleshooting de PR bloqueado, CI falhando, branch protection, conflitos:
→ Use a skill `github-project`. Ela cobre diagnóstico e resolução desses casos.

Para worktrees, branches, commits, push: git nativo + gh CLI.

Para leitura de pm-config.json: referência local `references/pm-config-schema.md`.

## Workflows disponíveis

| Trigger | Referência |
| ------- | ---------- |
| "criar ticket", "novo bug", "nova feature", "nova task" | `references/01-criar-issue.md` |
| "trabalhar em #N", "pegar #N", "criar worktree para #N" | `references/02-trabalhar-issue.md` |
| "fechar #N", "concluir", "marcar como done" | `references/03-fechar-issue.md` |
| "backlog", "o que está pendente", "listar issues", "o que tem pra fazer" | `references/04-backlog.md` |

Carregue a referência relevante antes de responder. Cada referência tem o passo a passo
completo — não invente um fluxo alternativo.

## Princípios

1. Todo trabalho de código deve ter issue. Se não tem → criar retroativamente antes de continuar.
2. Confirmar com o usuário antes de criar ou fechar issues.
3. Status reflete estado real. Nunca deixar In Progress se o trabalho parou.
4. NUNCA operar em `main` — sempre em branch de feature ou worktree.
5. NUNCA fazer merge sem gate `check-pr-status.sh` com exit 0 (ou exit 4 + confirmação).
6. NUNCA usar `--admin` bypass sem pedido explícito e confirmação dupla.

## Anti-padrões

- Não fechar issue sem validar critérios de aceite no body da issue.
- Não criar worktree de `main` sem `-b <branch>`.
- Não fazer merge sem CI verde (scripts/check-pr-status.sh).
- Não remover worktree com código não commitado (scripts/worktree-safety-check.sh).
```

- [ ] **Step 2: Create reference files**

`templates/skills/github-pm/references/01-criar-issue.md`:

```markdown
# Workflow 1: Criar issue

## Pré-condições
- Confirmar tipo: bug / feature / task
- Ter título e descrição mínima

## Passo a passo

1. Listar labels disponíveis para orientar o tipo:
   ```bash
   gh label list --repo <owner>/<repo>
   ```

2. Usar `github-issues` skill para criar:
   ```bash
   gh issue create \
     --title "<título conciso>" \
     --body "<descrição + critérios de aceite>" \
     --label "<bug|enhancement|task>" \
     --repo <owner>/<repo>
   ```

3. Adicionar ao Projects v2 (via `mcp__github__projects_write` ou gh CLI):
   ```bash
   # Ler project_id de .claude/pm-config.json
   gh project item-add <project_number> --owner <owner> --url <issue_url>
   ```

4. Definir status como Backlog (via GraphQL mutation — ver pm-config-schema.md).

5. Confirmar URL da issue criada ao usuário.

## Critérios de aceite

O body da issue deve ter:
- Descrição do problema ou funcionalidade
- Critérios de aceite (lista com checkboxes `- [ ]`)
- Contexto (opcional mas recomendado)
```

`templates/skills/github-pm/references/02-trabalhar-issue.md`:

```markdown
# Workflow 2: Trabalhar em uma issue

## Pré-condições
- Issue existe e está no Backlog
- `pm-config.json` disponível

## Passo a passo

1. Ler detalhes da issue:
   ```bash
   gh issue view <N> --json title,labels,body --repo <owner>/<repo>
   ```

2. Gerar slug da branch: `tipo/N-titulo-em-kebab-case`
   - tipo: `feat` (feature/enhancement), `fix` (bug), `chore` (task/infra), `docs`
   - Limitar a 60 chars; caracteres especiais → `-`
   - Exemplo: `feat/42-add-payment-flow`

3. Confirmar o nome da branch com o usuário.

4. Criar worktree (preferencialmente via `/pm:worktree-new <N>`):
   ```bash
   git worktree add .claude/worktrees/<SLUG> -b <SLUG>
   ```

5. Mover issue para In Progress no Projects v2 (usar IDs de pm-config.json):
   ```bash
   # GraphQL mutation — ver pm-config-schema.md para campos
   gh api graphql -f query='mutation {
     updateProjectV2ItemFieldValue(input: {
       projectId: "<project_id>"
       itemId: "<item_id>"
       fieldId: "<status_field_id>"
       value: { singleSelectOptionId: "<In Progress option ID>" }
     }) { projectV2Item { id } }
   }'
   ```

6. Comentar na issue: "🤖 Iniciando trabalho na branch `<SLUG>`"

## Invariantes
- NUNCA criar branch diretamente em main sem `-b`
- Se worktree já existe para o slug → perguntar se reabre
```

`templates/skills/github-pm/references/03-fechar-issue.md`:

```markdown
# Workflow 3: Fechar issue / marcar como Done

## Pré-condições
- PR mergeado (o auto-close-issue.yml fecha automaticamente; este workflow é manual)
- Ou: trabalho concluído sem PR (ex.: infra, documentação)

## Passo a passo

1. Ler body da issue e verificar critérios de aceite:
   ```bash
   gh issue view <N> --json body,title --repo <owner>/<repo>
   ```

2. Confirmar com o usuário quais critérios foram atendidos.

3. Se todos os critérios atendidos:
   ```bash
   gh issue close <N> --comment "Concluído. [descrever o que foi feito]" \
     --repo <owner>/<repo>
   ```

4. Mover para Done no Projects v2 (IDs de pm-config.json):
   ```bash
   gh api graphql -f query='mutation { updateProjectV2ItemFieldValue(input: {
     projectId: "<project_id>" itemId: "<item_id>"
     fieldId: "<status_field_id>"
     value: { singleSelectOptionId: "<Done option ID>" }
   }) { projectV2Item { id } } }'
   ```

5. Se em worktree → perguntar se remove com `/pm:worktree-remove`.

## Invariante
- NUNCA fechar sem validar critérios de aceite. Se critérios não foram definidos na issue,
  definir junto com o usuário antes de fechar.
```

`templates/skills/github-pm/references/04-backlog.md`:

```markdown
# Workflow 4: Ver backlog e issues pendentes

## Visualizações rápidas

Listar issues abertas (backlog geral):
```bash
gh issue list --repo <owner>/<repo> --state open \
  --json number,title,labels,assignees,milestone \
  --limit 30
```

Filtrar por label:
```bash
gh issue list --repo <owner>/<repo> --label "bug" --state open
```

Filtrar por assignee (meu trabalho):
```bash
gh issue list --repo <owner>/<repo> --assignee "@me" --state open
```

## Ver projetos v2

```bash
gh project list --owner <owner> --format json
gh project item-list <project_number> --owner <owner> --format json
```

## Priorização

Ao apresentar o backlog ao usuário:
1. Agrupar por status atual (Triage / Backlog / In Progress / In Review)
2. Destacar Issues "In Progress" sem atividade recente (possível abandono)
3. Destacar Issues "Triage" sem triagem há mais de 3 dias

Nunca reordenar silenciosamente — apresentar e perguntar ao usuário.
```

`templates/skills/github-pm/references/pm-config-schema.md`:

```markdown
# Schema de pm-config.json

Arquivo em `.claude/pm-config.json` do projeto-alvo. Gerado por `/pm:setup-project`.

## Estrutura completa

```json
{
  "owner": "org-ou-usuario",
  "repo": "nome-do-repo",
  "project_number": 1,
  "project_id": "PVT_...",
  "status_field_id": "PVTSSF_...",
  "status_options": {
    "Triage":      "PVTSSFO_...",
    "Backlog":     "PVTSSFO_...",
    "In Progress": "PVTSSFO_...",
    "In Review":   "PVTSSFO_...",
    "Done":        "PVTSSFO_..."
  }
}
```

## Como usar os IDs nas mutations GraphQL

```bash
# Mover item para "In Progress":
ITEM_ID=$(gh api graphql -f query='
  query($proj: ID!, $after: String) {
    node(id: $proj) {
      ... on ProjectV2 {
        items(first: 100, after: $after) {
          nodes { id content { ... on Issue { number } } }
        }
      }
    }
  }' -F proj="<project_id>" --jq ".data.node.items.nodes[] | select(.content.number == <N>) | .id")

gh api graphql -f query='mutation {
  updateProjectV2ItemFieldValue(input: {
    projectId: "<project_id>"
    itemId: "'"$ITEM_ID"'"
    fieldId: "<status_field_id>"
    value: { singleSelectOptionId: "<In Progress ID from status_options>" }
  }) { projectV2Item { id } }
}'
```

## Adicionar issue ao projeto

```bash
ISSUE_URL=$(gh issue view <N> --json url --jq .url --repo <owner>/<repo>)
gh project item-add <project_number> --owner <owner> --url "$ISSUE_URL"
```
```

- [ ] **Step 3: Commit**

```bash
git add templates/skills/github-pm/
git commit -m "feat: add first-party github-pm skill + references"
```

---

## Task 4: Commands básicos (5 commands)

**Files:**
- Create: `templates/commands/pm/issue-new.md`
- Create: `templates/commands/pm/issue-work.md`
- Create: `templates/commands/pm/issue-close.md`
- Create: `templates/commands/pm/backlog.md`
- Create: `templates/commands/pm/setup-project.md`

- [ ] **Step 1: Create directory**

```bash
mkdir -p templates/commands/pm
```

- [ ] **Step 2: Write `issue-new.md`**

```markdown
---
description: Create a new GitHub issue and add it to the project board
argument-hint: "[description]"
allowed-tools: Bash(gh *), Bash(git *), Write
---

Config PM: !`cat .claude/pm-config.json 2>/dev/null || echo "NOT_FOUND"`
Remote: !`git remote get-url origin 2>/dev/null || echo "unknown"`

Use a skill `github-pm` para executar o workflow de criação de issue (Workflow 1).
Para CRUD da issue, a skill `github-issues` fornece as ferramentas MCP necessárias.

Se argumento fornecido (`$ARGUMENTS`), use como título inicial da issue.
Confirme título, tipo (bug/feature/task) e critérios de aceite com o usuário antes de criar.
```

- [ ] **Step 3: Write `issue-work.md`**

```markdown
---
description: Start working on a GitHub issue — set In Progress + open worktree
argument-hint: "[issue-number]"
allowed-tools: Bash(gh *), Bash(git *)
---

Issue: !`gh issue view ${ARGUMENTS:-} --json number,title,labels,body 2>/dev/null || echo "NOT_FOUND"`
Config PM: !`cat .claude/pm-config.json 2>/dev/null || echo "NOT_FOUND"`
Worktrees existentes: !`git worktree list 2>/dev/null`

Use a skill `github-pm` para executar o workflow de início de trabalho (Workflow 2).
Número da issue: `$ARGUMENTS`.

A skill irá:
1. Ler detalhes da issue
2. Sugerir nome da branch (tipo/N-slug)
3. Criar worktree em .claude/worktrees/
4. Mover issue para In Progress no Projects v2
5. Comentar na issue
```

- [ ] **Step 4: Write `issue-close.md`**

```markdown
---
description: Close an issue after validating acceptance criteria
argument-hint: "[issue-number]"
allowed-tools: Bash(gh *)
---

Issue: !`gh issue view ${ARGUMENTS:-} --json number,title,body,state 2>/dev/null || echo "NOT_FOUND"`
Config PM: !`cat .claude/pm-config.json 2>/dev/null || echo "NOT_FOUND"`

Use a skill `github-pm` para executar o workflow de fechamento de issue (Workflow 3).
Número da issue: `$ARGUMENTS`.

OBRIGATÓRIO: validar critérios de aceite no body antes de fechar. Nunca fechar sem esta validação.
```

- [ ] **Step 5: Write `backlog.md`**

```markdown
---
description: Show open issues grouped by Projects v2 status
allowed-tools: Bash(gh *)
---

Config PM: !`cat .claude/pm-config.json 2>/dev/null || echo "NOT_FOUND"`
Remote: !`git remote get-url origin 2>/dev/null || echo "unknown"`

Use a skill `github-pm` para executar o workflow de visualização de backlog (Workflow 4).

Apresente as issues agrupadas por status (Triage / Backlog / In Progress / In Review).
Destaque issues In Progress sem atividade recente e issues Triage há mais de 3 dias.
```

- [ ] **Step 6: Write `setup-project.md`**

```markdown
---
description: Interactive setup — link this repo to a GitHub Project and write pm-config.json
allowed-tools: Bash(gh *), Bash(git *), Write
---

Auth status: !`gh auth status 2>&1 | head -5`
Remote: !`git remote get-url origin 2>/dev/null || echo "unknown"`
Config PM atual: !`cat .claude/pm-config.json 2>/dev/null || echo "NOT_FOUND"`

Configure o GitHub PM para este repositório. Execute os seguintes passos:

1. Verificar autenticação: `gh auth status`. Se não autenticado → `gh auth login` e parar.

2. Extrair owner e repo do remote URL.

3. Listar projetos disponíveis:
   ```bash
   gh project list --owner <owner> --format json --limit 20
   ```
   Apresentar ao usuário e pedir que selecione o número do projeto.

4. Buscar IDs via GraphQL:
   ```bash
   gh api graphql -f query='
     query($owner: String!, $num: Int!) {
       user(login: $owner) {
         projectV2(number: $num) {
           id
           fields(first: 20) {
             nodes {
               ... on ProjectV2SingleSelectField {
                 id name options { id name }
               }
             }
           }
         }
       }
     }' -F owner=<owner> -F num=<project_number>
   ```
   Identificar o campo "Status" e extrair IDs de cada opção
   (Triage, Backlog, In Progress, In Review, Done).

5. Escrever `.claude/pm-config.json` com os IDs reais. Usar Write tool.

6. Verificar se `PROJECTS_PAT` existe como secret do repo:
   ```bash
   gh secret list --repo <owner>/<repo> 2>/dev/null | grep PROJECTS_PAT || echo "NOT_SET"
   ```
   Se NOT_SET → instruir: "Crie um PAT com escopos `repo` e `project` em
   github.com/settings/tokens, depois: `gh secret set PROJECTS_PAT --repo <owner>/<repo>`"

7. Confirmar: "GitHub PM configurado. Rode `/pm:backlog` para ver o backlog."
```

- [ ] **Step 7: Commit**

```bash
git add templates/commands/pm/
git commit -m "feat: add 5 basic /pm:* commands"
```

---

## Task 5: Commands dev loop (5 commands)

**Files:**
- Create: `templates/commands/pm/worktree-new.md`
- Create: `templates/commands/pm/commit-push-pr.md`
- Create: `templates/commands/pm/pr-merge.md`
- Create: `templates/commands/pm/worktree-remove.md`
- Create: `templates/commands/pm/code-review-pr.md`

- [ ] **Step 1: Write `worktree-new.md`**

```markdown
---
description: Create an isolated git worktree for an issue with auto-named branch
argument-hint: "[issue-number]"
allowed-tools: Bash(gh *), Bash(git *), AskUserQuestion
---

Issue: !`gh issue view ${ARGUMENTS:-} --json number,title,labels 2>/dev/null || echo "NOT_FOUND"`
Config PM: !`cat .claude/pm-config.json 2>/dev/null || echo "NOT_FOUND"`
Worktrees existentes: !`git worktree list 2>/dev/null`
Branch atual: !`git branch --show-current`

Use a skill `github-pm` para executar o workflow de criação de worktree:

1. Ler `gh issue view $ARGUMENTS --json title,labels` para determinar tipo e título.
2. Gerar slug: tipo/N-titulo-em-kebab-case (máx 60 chars; especiais → `-`)
   - tipo: `feat` (feature/enhancement), `fix` (bug), `chore` (task), `docs`
   - Exemplo: `feat/42-add-payment-flow`
3. Confirmar nome da branch com o usuário via AskUserQuestion.
4. Se worktree já existe para o slug → avisar e perguntar se reabre.
5. Criar worktree:
   ```bash
   git worktree add .claude/worktrees/<SLUG> -b <SLUG>
   ```
6. Entrar na worktree via EnterWorktree (se disponível) ou instruir o usuário.
7. Comentar na issue: "🤖 Worktree criada: branch `<SLUG>`"
8. Mover issue para In Progress no Projects v2 (usar pm-config.json).

REGRAS:
- NUNCA criar sem `-b <SLUG>` (sempre nova branch)
- NUNCA partir de main sem criar branch separada
```

- [ ] **Step 2: Write `commit-push-pr.md`**

```markdown
---
description: Commit changes, push branch, and open a PR linked to the current issue
allowed-tools: Bash(git *), Bash(gh *)
---

Branch atual: !`git branch --show-current`
Status: !`git status --short`
Diff: !`git diff HEAD --stat`
Config PM: !`cat .claude/pm-config.json 2>/dev/null || echo "NOT_FOUND"`
Issues linked (via branch name): !`git branch --show-current | grep -oE '[0-9]+' | head -1 | xargs -I{} gh issue view {} --json number,title 2>/dev/null || echo "none"`

Use a skill `github-pm` para executar este workflow:

1. GATE OBRIGATÓRIO: se branch atual = `main` ou `master` → PARAR.
   Instruir o usuário a criar uma branch primeiro (`/pm:worktree-new` ou `git checkout -b`).

2. Analisar `git diff HEAD` para gerar mensagem de commit (conventional commits):
   - feat: nova funcionalidade
   - fix: correção de bug
   - chore: manutenção/infra
   - docs: documentação
   - refactor: refatoração sem mudança de comportamento
   - test: testes

3. Propor commit message e mostrar ao usuário. Aguardar confirmação.

4. Commitar:
   ```bash
   git add -A && git commit -m "<mensagem aprovada>"
   ```

5. Push (criar upstream se necessário):
   ```bash
   git push -u origin <BRANCH> 2>/dev/null || git push origin <BRANCH>
   ```

6. Detectar issue pelo nome da branch (ex: `feat/42-*` → issue #42).
   Criar PR com "Closes #N" no body se issue detectada:
   ```bash
   gh pr create \
     --title "<título baseado no commit>" \
     --body "## Resumo\n\n<descrição>\n\nCloses #<N>" \
     --base main
   ```

7. Reportar URL do PR. Sugerir: "Rode `/pm:code-review-pr <PR>` para iniciar review."
```

- [ ] **Step 3: Write `pr-merge.md`**

```markdown
---
description: Safely merge a PR — validates CI green before merging, never bypasses checks
argument-hint: "[pr-or-issue-number]"
allowed-tools: Bash(gh *), Bash(git *), Bash(bash *), Bash(python3 *)
---

Config PM: !`cat .claude/pm-config.json 2>/dev/null || echo "NOT_FOUND"`
Branch atual: !`git branch --show-current`

Use a skill `github-pm` para executar o merge seguro. O argumento `$ARGUMENTS`
pode ser número de PR ou número de issue.

**NUNCA pule os gates abaixo. Esta é a sequência obrigatória:**

**Passo 1 — Identificar PR**
- Tentar direto como PR number: `gh pr view $ARGUMENTS --json number`
- Se falhar: buscar PR com branch `feat/$ARGUMENTS-*` ou body com `Closes #$ARGUMENTS`
  ```bash
  gh pr list --json number,headRefName,body | python3 -c "
  import sys,json; prs=json.load(sys.stdin)
  n=int('$ARGUMENTS')
  for p in prs:
    if f'/{n}-' in p['headRefName'] or f'#{n}' in p.get('body',''):
      print(p['number']); break
  "
  ```

**Passo 2 — Verificar draft**
```bash
gh pr view $PR_NUMBER --json isDraft --jq '.isDraft'
```
Se true → perguntar ao usuário se marca como ready:
```bash
gh pr ready $PR_NUMBER
```
Depois aguardar CI: `gh pr checks $PR_NUMBER --watch`

**Passo 3 — Gate autoritativo (SEMPRE executar, NUNCA pular)**
```bash
bash .claude/skills/github-pm/scripts/check-pr-status.sh $PR_NUMBER <OWNER>/<REPO>
```
- Exit 0 → prosseguir
- Exit 1 → BLOQUEAR. Listar falhas, não merge.
- Exit 2 → perguntar se aguarda (`gh pr checks $PR_NUMBER --watch`), re-rodar gate
- Exit 3 → ENCERRAR (PR inválido)
- Exit 4 → avisar "CI verde mas sem review aprovado". Perguntar se prossegue mesmo assim.

**Passo 4 — Detectar estratégia de merge**
```bash
gh repo view --json squashMergeAllowed,rebaseMergeAllowed,mergeCommitAllowed \
  --jq 'if .squashMergeAllowed then "--squash" elif .rebaseMergeAllowed then "--rebase" else "--merge" end'
```

**Passo 5 — Merge**
```bash
gh pr merge $PR_NUMBER $MERGE_FLAG --delete-branch
```
Se exit ≠ 0 → reportar erro exato, ENCERRAR sem pós-merge.

**Passo 6 — Pós-merge (somente se merge com exit 0)**
- Comentar na issue: "PR #$PR_NUMBER mergeado ✅"
- Fechar issue: `gh issue close $ISSUE_NUMBER --repo <OWNER>/<REPO>`
- Mover para Done no Projects v2 (usar pm-config.json)

**Passo 7 — Cleanup**
- Se em worktree → perguntar: "Deseja remover a worktree? (`/pm:worktree-remove`)"
- Se no checkout principal: `git checkout main && git pull --ff-only`

REGRAS CRÍTICAS (nunca violar):
- NUNCA `gh pr merge` sem gate do Passo 3 com exit 0 (ou exit 4 + confirmação explícita)
- NUNCA fechar issue antes de confirmar exit 0 do merge
- NUNCA `--admin` sem pedido explícito + confirmação dupla
```

- [ ] **Step 4: Write `worktree-remove.md`**

```markdown
---
description: Safely remove a worktree — validates no uncommitted work, CI, and merge status
argument-hint: "[branch|issue-number|path]"
allowed-tools: Bash(gh *), Bash(git *), Bash(bash *)
---

Config PM: !`cat .claude/pm-config.json 2>/dev/null || echo "NOT_FOUND"`
Worktrees: !`git worktree list 2>/dev/null`
Branch atual: !`git branch --show-current`

Use a skill `github-pm` para remover a worktree com segurança.
Argumento: `$ARGUMENTS` (branch, número de issue, path, ou vazio para worktree atual).

**NUNCA pule os gates abaixo:**

**Passo 1 — Gate de segurança**
```bash
bash .claude/skills/github-pm/scripts/worktree-safety-check.sh \
  "$ARGUMENTS" "<OWNER>/<REPO>"
```
- Exit 0 → capturar RESULT_WT_PATH e RESULT_WT_BRANCH do stdout, prosseguir
- Exit 1 → BLOQUEAR. Mostrar checklist ✅/❌. Encerrar sem remover.
- Exit 2 → worktree não encontrada. Listar worktrees disponíveis com `git worktree list`.

**Passo 2 — Sair da worktree (se sessão está dentro dela)**
Verificar se `$CLAUDE_WORKTREE_PATH` corresponde ao WT_PATH.
Se sim: ExitWorktree com action "keep" antes de qualquer remoção.

**Passo 3 — Gate 2: checkout principal limpo**
```bash
MAIN_ROOT=$(git rev-parse --show-toplevel)
git -C "$MAIN_ROOT" status --porcelain
```
Se sujo → ABORTAR: "Checkout principal tem mudanças não salvas."
```bash
git -C "$MAIN_ROOT" checkout main && git -C "$MAIN_ROOT" pull --ff-only
```

**Passo 4 — Remover**
```bash
git worktree remove --force "$RESULT_WT_PATH"
git branch -D "$RESULT_WT_BRANCH"
rm -rf "$RESULT_WT_PATH"
git worktree prune
```

**Passo 5 — Confirmar**
```bash
git worktree list
```
Informar ao usuário que a worktree foi removida.

REGRAS CRÍTICAS:
- NUNCA `rm -rf` antes do Passo 2 (sair primeiro)
- NUNCA remover com checklist vermelho — listar o que falta
- NÃO deletar branch remota (já removida pelo merge com --delete-branch)
```

- [ ] **Step 5: Write `code-review-pr.md`**

```markdown
---
description: Parallel multi-perspective code review of a PR using subagents
argument-hint: "[pr-number]"
allowed-tools: Bash(gh *), Bash(bash *), Bash(python3 *)
---

Config PM: !`cat .claude/pm-config.json 2>/dev/null || echo "NOT_FOUND"`
PR info: !`gh pr view ${ARGUMENTS:-} --json number,title,state,isDraft,headRefName,body 2>/dev/null || echo "NOT_FOUND"`
Diff stat: !`gh pr diff ${ARGUMENTS:-} --stat 2>/dev/null || echo "NOT_FOUND"`

Use a skill `github-pm` para executar o code review paralelo.
Número do PR: `$ARGUMENTS`.

**Sequência obrigatória:**

1. **Haiku agent** → elegibilidade: PR fechado? Já revisado sem fixes pendentes? Automatizado?
   Se não elegível → reportar motivo e encerrar.

2. **Haiku agent** → listar CLAUDE.md relevantes do codebase.

3. **Haiku agent** → resumo das mudanças do PR (arquivos, propósito, escopo).

4. **6 agentes Sonnet em paralelo** (dispatch único, não sequencial):
   - #1: Conformidade com CLAUDE.md do projeto
   - #2: Scan de bugs nas linhas modificadas (somente diff, não todo o arquivo)
   - #3: Git blame e histórico dos arquivos modificados
   - #4: PRs anteriores que tocaram os mesmos arquivos
   - #5: Comentários inline nos arquivos modificados (TODO, FIXME, HACK)
   - #6: Over-engineering, YAGNI, duplicações, abstrações desnecessárias

5. Para cada issue encontrada: **Haiku agent de scoring** (0-100).
   Critérios: severidade, certeza, impacto no usuário.

6. Filtrar: manter apenas issues com score ≥ 60.

7. **Re-check de elegibilidade** (Haiku): alguma issue já foi corrigida entre os passos?

8. Postar resultado no PR:
   ```bash
   gh pr comment $ARGUMENTS --body "<resultado formatado com links file:sha#L>"
   ```
   Formato: sem emojis; links `owner/repo/blob/<sha>/<file>#L<line>`; agrupado por severidade.

9. Mensagem terminal:
   - Issues encontradas → "Para corrigir automaticamente: `/orchestrate`"
   - Nenhuma issue + CI verde → "PR pronto para merge: `/pm:pr-merge $ARGUMENTS`"
   - Nenhuma issue + CI pendente/falhando → informar e não sugerir merge
```

- [ ] **Step 6: Commit**

```bash
git add templates/commands/pm/
git commit -m "feat: add 5 dev-loop /pm:* commands (worktree, commit/PR, merge, review)"
```

---

## Task 6: GitHub templates + Actions + pm-config template

**Files:**
- Create: `templates/github/ISSUE_TEMPLATE/bug.yml`
- Create: `templates/github/ISSUE_TEMPLATE/feature.yml`
- Create: `templates/github/ISSUE_TEMPLATE/task.yml`
- Create: `templates/github/PULL_REQUEST_TEMPLATE.md`
- Create: `templates/github/workflows/issue-to-project.yml`
- Create: `templates/github/workflows/commit-to-progress.yml`
- Create: `templates/github/workflows/pr-to-review.yml`
- Create: `templates/github/workflows/auto-close-issue.yml`
- Create: `templates/github/pm-config.json.template`

- [ ] **Step 1: Create directories**

```bash
mkdir -p templates/github/ISSUE_TEMPLATE
mkdir -p templates/github/workflows
```

- [ ] **Step 2: Issue templates**

`templates/github/ISSUE_TEMPLATE/bug.yml`:
```yaml
name: Bug Report
description: Report a bug or unexpected behavior
labels: ["bug", "needs-triage"]
body:
  - type: markdown
    attributes:
      value: "**Obrigado por reportar!** Preencha os campos abaixo."
  - type: textarea
    id: description
    attributes:
      label: Descrição do problema
      description: O que aconteceu? O que era esperado?
    validations:
      required: true
  - type: textarea
    id: steps
    attributes:
      label: Passos para reproduzir
      placeholder: |
        1. Faça X
        2. Clique em Y
        3. Veja o erro
    validations:
      required: true
  - type: textarea
    id: acceptance
    attributes:
      label: Critérios de aceite
      description: Como saberemos que o bug foi corrigido?
      placeholder: |
        - [ ] Comportamento esperado funcionando
        - [ ] Sem regressão em casos de uso adjacentes
    validations:
      required: true
  - type: textarea
    id: context
    attributes:
      label: Contexto adicional
      description: Versão, ambiente, logs, screenshots
```

`templates/github/ISSUE_TEMPLATE/feature.yml`:
```yaml
name: Feature Request
description: Suggest a new feature or enhancement
labels: ["enhancement", "needs-triage"]
body:
  - type: textarea
    id: description
    attributes:
      label: Descrição da feature
      description: O que deve ser possível fazer que não é possível hoje?
    validations:
      required: true
  - type: textarea
    id: motivation
    attributes:
      label: Motivação
      description: Por que esta feature é importante? Qual problema resolve?
    validations:
      required: true
  - type: textarea
    id: acceptance
    attributes:
      label: Critérios de aceite
      placeholder: |
        - [ ] Usuário consegue fazer X
        - [ ] Edge case Y tratado corretamente
    validations:
      required: true
```

`templates/github/ISSUE_TEMPLATE/task.yml`:
```yaml
name: Task / Chore
description: Technical task, infrastructure, or maintenance
labels: ["task"]
body:
  - type: textarea
    id: description
    attributes:
      label: Descrição da tarefa
    validations:
      required: true
  - type: textarea
    id: acceptance
    attributes:
      label: Critérios de aceite
      placeholder: |
        - [ ] Tarefa concluída
        - [ ] Documentação atualizada se necessário
    validations:
      required: true
```

- [ ] **Step 3: PR template**

`templates/github/PULL_REQUEST_TEMPLATE.md`:
```markdown
## Resumo

<!-- Descreva o que esta PR faz e por quê -->

## Issues relacionadas

<!-- Liste as issues que esta PR resolve -->
Closes #

## Tipo de mudança

- [ ] Bug fix
- [ ] Nova feature
- [ ] Refatoração (sem mudança de comportamento)
- [ ] Documentação
- [ ] Outro

## Checklist

- [ ] Código segue os padrões do projeto (CLAUDE.md)
- [ ] Testes adicionados/atualizados para a mudança
- [ ] Sem `console.log` ou debug code residual
- [ ] Self-review realizado

## Como testar

<!-- Passos para validar esta PR -->
```

- [ ] **Step 4: GitHub Actions workflows**

`templates/github/workflows/issue-to-project.yml`:
```yaml
name: Issue → Project (Triage)
on:
  issues:
    types: [opened]

jobs:
  add-to-project:
    runs-on: ubuntu-latest
    steps:
      - name: Check PROJECTS_PAT
        run: |
          if [ -z "${{ secrets.PROJECTS_PAT }}" ]; then
            echo "PROJECTS_PAT not set — skipping project automation"
            exit 0
          fi

      - name: Add issue to project
        if: secrets.PROJECTS_PAT != ''
        uses: actions/add-to-project@v1.0.2
        with:
          project-url: https://github.com/orgs/${{ github.repository_owner }}/projects/1
          github-token: ${{ secrets.PROJECTS_PAT }}

      - name: Set status to Triage
        if: secrets.PROJECTS_PAT != ''
        uses: github/update-project-action@v1
        with:
          github_token: ${{ secrets.PROJECTS_PAT }}
          organization: ${{ github.repository_owner }}
          project_number: 1
          item_url: ${{ github.event.issue.html_url }}
          field: Status
          value: Triage

      - name: Add needs-triage label
        if: secrets.PROJECTS_PAT != ''
        uses: andymckay/labeler@master
        with:
          add-labels: "needs-triage"
          github-token: ${{ secrets.PROJECTS_PAT }}
```

`templates/github/workflows/commit-to-progress.yml`:
```yaml
name: Commit → In Progress
on:
  push:
    branches-ignore:
      - main
      - master

jobs:
  set-in-progress:
    runs-on: ubuntu-latest
    if: "!startsWith(github.event.head_commit.message, '[skip-pm]')"
    steps:
      - name: Check PROJECTS_PAT
        run: |
          if [ -z "${{ secrets.PROJECTS_PAT }}" ]; then
            echo "PROJECTS_PAT not set — skipping"
            exit 0
          fi

      - name: Extract issue numbers from commits
        id: issues
        run: |
          ISSUES=$(echo "${{ github.event.commits[0].message }}" | grep -oE '#[0-9]+' | tr -d '#' | tr '\n' ',' | sed 's/,$//')
          echo "issue_numbers=$ISSUES" >> $GITHUB_OUTPUT

      - name: Move linked issues to In Progress
        if: steps.issues.outputs.issue_numbers != '' && secrets.PROJECTS_PAT != ''
        env:
          GH_TOKEN: ${{ secrets.PROJECTS_PAT }}
          REPO: ${{ github.repository }}
        run: |
          IFS=',' read -ra NUMS <<< "${{ steps.issues.outputs.issue_numbers }}"
          for N in "${NUMS[@]}"; do
            # Check if issue has pm:paused label — skip if so
            LABELS=$(gh issue view "$N" --repo "$REPO" --json labels --jq '[.labels[].name]')
            if echo "$LABELS" | grep -q '"pm:paused"'; then
              echo "Issue #$N has pm:paused — skipping"
              continue
            fi
            echo "Moving issue #$N to In Progress"
            # Note: update via GraphQL using pm-config IDs is done by the skill;
            # this workflow uses the project CLI shorthand
            gh project item-edit --id "$(gh project item-list 1 --owner "${{ github.repository_owner }}" \
              --format json | python3 -c "
import sys,json; items=json.load(sys.stdin)['items']
for i in items:
  if i.get('content',{}).get('number') == $N: print(i['id']); break
")" --field-id Status --project-id 1 --single-select-option-id "In Progress" \
              --owner "${{ github.repository_owner }}" 2>/dev/null || echo "Could not update #$N"
          done
```

`templates/github/workflows/pr-to-review.yml`:
```yaml
name: PR → In Review
on:
  pull_request:
    types: [opened, ready_for_review, reopened]

jobs:
  set-in-review:
    runs-on: ubuntu-latest
    if: "!github.event.pull_request.draft && !contains(github.event.pull_request.body, '[skip-pm]')"
    steps:
      - name: Check PROJECTS_PAT
        run: |
          if [ -z "${{ secrets.PROJECTS_PAT }}" ]; then
            echo "PROJECTS_PAT not set — skipping"
            exit 0
          fi

      - name: Extract linked issues from PR body
        id: issues
        run: |
          ISSUES=$(echo "${{ github.event.pull_request.body }}" | \
            grep -ioE '(closes|fixes|resolves)\s+#[0-9]+' | \
            grep -oE '#[0-9]+' | tr -d '#' | tr '\n' ',' | sed 's/,$//')
          echo "issue_numbers=$ISSUES" >> $GITHUB_OUTPUT

      - name: Move linked issues to In Review
        if: steps.issues.outputs.issue_numbers != '' && secrets.PROJECTS_PAT != ''
        env:
          GH_TOKEN: ${{ secrets.PROJECTS_PAT }}
        run: |
          IFS=',' read -ra NUMS <<< "${{ steps.issues.outputs.issue_numbers }}"
          for N in "${NUMS[@]}"; do
            echo "Moving issue #$N to In Review"
            # Status update via gh CLI (see commit-to-progress.yml for pattern)
          done
```

`templates/github/workflows/auto-close-issue.yml`:
```yaml
name: Auto-close issue on PR merge
on:
  pull_request:
    types: [closed]

jobs:
  close-linked-issues:
    runs-on: ubuntu-latest
    if: github.event.pull_request.merged == true
    steps:
      - name: Check PROJECTS_PAT
        run: |
          if [ -z "${{ secrets.PROJECTS_PAT }}" ]; then
            echo "PROJECTS_PAT not set — skipping"
            exit 0
          fi

      - name: Extract linked issues
        id: issues
        run: |
          ISSUES=$(echo "${{ github.event.pull_request.body }}" | \
            grep -ioE '(closes|fixes|resolves)\s+#[0-9]+' | \
            grep -oE '#[0-9]+' | tr -d '#' | tr '\n' ',' | sed 's/,$//')
          echo "issue_numbers=$ISSUES" >> $GITHUB_OUTPUT

      - name: Close issues and move to Done
        if: steps.issues.outputs.issue_numbers != '' && secrets.PROJECTS_PAT != ''
        env:
          GH_TOKEN: ${{ secrets.PROJECTS_PAT }}
          REPO: ${{ github.repository }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
        run: |
          IFS=',' read -ra NUMS <<< "${{ steps.issues.outputs.issue_numbers }}"
          for N in "${NUMS[@]}"; do
            echo "Closing issue #$N and moving to Done"
            gh issue close "$N" \
              --comment "Fechado automaticamente pelo merge da PR #$PR_NUMBER. ✅" \
              --repo "$REPO" 2>/dev/null || echo "Could not close #$N"
            # Move to Done via project CLI (same pattern as commit-to-progress.yml)
          done
```

- [ ] **Step 5: pm-config template**

`templates/github/pm-config.json.template`:
```json
{
  "_readme": "Generated by /pm:setup-project. Replace placeholders with real IDs from your GitHub project.",
  "_setup": "Run /pm:setup-project to auto-populate all IDs. Or run: gh project list --owner <owner>",
  "_pat": "Create PROJECTS_PAT secret: gh secret set PROJECTS_PAT --repo <owner>/<repo>",
  "owner": "YOUR_ORG_OR_USER",
  "repo": "YOUR_REPO_NAME",
  "project_number": 1,
  "project_id": "PVT_REPLACE_ME",
  "status_field_id": "PVTSSF_REPLACE_ME",
  "status_options": {
    "Triage":      "PVTSSFO_REPLACE_ME",
    "Backlog":     "PVTSSFO_REPLACE_ME",
    "In Progress": "PVTSSFO_REPLACE_ME",
    "In Review":   "PVTSSFO_REPLACE_ME",
    "Done":        "PVTSSFO_REPLACE_ME"
  }
}
```

- [ ] **Step 6: Commit**

```bash
git add templates/github/
git commit -m "feat: add GitHub issue/PR templates, 4 Actions workflows, pm-config template"
```

---

## Task 7: Vendoring infrastructure

**Files:**
- Read: `scripts/sync-ecc.mjs` (understand fetch + transform + stamp pattern)
- Read: `lib/ecc/transform.mjs` (understand pure transform pattern)
- Create: `scripts/github-issues-source.json`
- Create: `scripts/github-project-source.json`
- Create: `scripts/sync-github-issues.mjs`
- Create: `scripts/sync-github-project.mjs`
- Create: `lib/github-pm/transform.mjs`
- Create: `templates/github-pm-ext/MANIFEST.json`
- Modify: `package.json`

**Interfaces:**
- Produces: `templates/github-pm-ext/github-issues/` populated with vendored SKILL.md
- Produces: `templates/github-pm-ext/github-project/` populated with vendored SKILL.md
- Consumed by: Task 8 catalog (copyFrom paths)

- [ ] **Step 1: Read existing sync pattern**

```bash
cat scripts/sync-ecc.mjs
cat lib/ecc/transform.mjs
```

Study: how it fetches from GitHub API, writes files, calls transform, writes MANIFEST.

- [ ] **Step 2: Pin upstream commits**

Find current HEAD commits of upstream repos:
```bash
# github-issues from awesome-copilot
curl -s "https://api.github.com/repos/github/awesome-copilot/commits?path=skills/github-issues&per_page=1" \
  | python3 -c "import sys,json; print(json.load(sys.stdin)[0]['sha'])"

# github-project from netresearch
curl -s "https://api.github.com/repos/netresearch/github-project-skill/commits?per_page=1" \
  | python3 -c "import sys,json; print(json.load(sys.stdin)[0]['sha'])"
```

- [ ] **Step 3: Write `scripts/github-issues-source.json`**

Use the commit SHA from Step 2:
```json
{
  "repo": "github/awesome-copilot",
  "commit": "<SHA_FROM_STEP_2>",
  "path": "skills/github-issues",
  "license": "MIT",
  "description": "GitHub Issues skill — CRUD, Projects v2, sub-issues, search"
}
```

- [ ] **Step 4: Write `scripts/github-project-source.json`**

```json
{
  "repo": "netresearch/github-project-skill",
  "commit": "<SHA_FROM_STEP_2>",
  "path": ".",
  "license": "MIT AND CC-BY-SA-4.0",
  "description": "GitHub Project skill — PR/CI troubleshooting, branch protection, merge diagnosis"
}
```

- [ ] **Step 5: Write `lib/github-pm/transform.mjs`**

Read `lib/ecc/transform.mjs` first. Then adapt for SKILL.md frontmatter:

```js
/**
 * Pure transform for vendored github-pm-ext skills.
 * No IO — takes text, returns text. Tested without filesystem.
 * @module github-pm/transform
 */

/**
 * @param {string} content  Raw SKILL.md content from upstream
 * @param {Object} meta
 * @param {string} meta.repo      e.g. "github/awesome-copilot"
 * @param {string} meta.commit    pinned SHA
 * @param {string} meta.filePath  path within the repo
 * @param {string} meta.license
 * @returns {string}
 */
export function transformSkill(content, { repo, commit, filePath, license }) {
  const provenance = `<!-- vendored from https://github.com/${repo}/blob/${commit}/${filePath} | license: ${license} -->`;

  // Split frontmatter if present
  const FM_RE = /^---\n([\s\S]*?)\n---\n/;
  const match = content.match(FM_RE);
  if (match) {
    const fm = match[1];
    const body = content.slice(match[0].length);
    return `---\n${fm}\n---\n\n${provenance}\n\n${body}`;
  }

  return `${provenance}\n\n${content}`;
}
```

- [ ] **Step 6: Write `scripts/sync-github-issues.mjs`**

Read `scripts/sync-ecc.mjs` to understand the exact fetch pattern, then adapt:

```js
#!/usr/bin/env node
/**
 * Vendor github-issues skill from github/awesome-copilot.
 * Run: npm run sync:github-issues
 */
import path from "node:path";
import fs from "node:fs/promises";
import { fileURLToPath } from "node:url";
import { transformSkill } from "../lib/github-pm/transform.mjs";

const ROOT = path.join(path.dirname(fileURLToPath(import.meta.url)), "..");
const SOURCE_FILE = path.join(ROOT, "scripts", "github-issues-source.json");
const OUT_DIR = path.join(ROOT, "templates", "github-pm-ext", "github-issues");
const MANIFEST_FILE = path.join(ROOT, "templates", "github-pm-ext", "MANIFEST.json");

const source = JSON.parse(await fs.readFile(SOURCE_FILE, "utf8"));
const { repo, commit, path: srcPath, license } = source;

const BASE_URL = `https://raw.githubusercontent.com/${repo}/${commit}/${srcPath}`;

async function fetchText(url) {
  const res = await fetch(url);
  if (!res.ok) throw new Error(`HTTP ${res.status} fetching ${url}`);
  return res.text();
}

async function listFiles(apiPath) {
  const url = `https://api.github.com/repos/${repo}/contents/${apiPath}?ref=${commit}`;
  const res = await fetch(url, { headers: { Accept: "application/vnd.github.v3+json" } });
  if (!res.ok) throw new Error(`HTTP ${res.status} listing ${url}`);
  return res.json();
}

await fs.rm(OUT_DIR, { recursive: true, force: true });
await fs.mkdir(OUT_DIR, { recursive: true });

// Fetch directory listing
const entries = await listFiles(srcPath);
for (const entry of entries) {
  if (entry.type === "file") {
    const raw = await fetchText(entry.download_url);
    const transformed = entry.name.endsWith(".md")
      ? transformSkill(raw, { repo, commit, filePath: entry.path, license })
      : raw;
    await fs.writeFile(path.join(OUT_DIR, entry.name), transformed, "utf8");
    console.log(`  ✓ ${entry.name}`);
  }
}

// Update MANIFEST
let manifest = {};
try { manifest = JSON.parse(await fs.readFile(MANIFEST_FILE, "utf8")); } catch {}
manifest["github-issues"] = { repo, commit, license, path: srcPath };
await fs.writeFile(MANIFEST_FILE, JSON.stringify(manifest, null, 2) + "\n", "utf8");

console.log("sync:github-issues done");
```

- [ ] **Step 7: Write `scripts/sync-github-project.mjs`**

Adapt from Step 6, targeting `netresearch/github-project-skill`:

```js
#!/usr/bin/env node
/**
 * Vendor github-project skill from netresearch/github-project-skill.
 */
import path from "node:path";
import fs from "node:fs/promises";
import { fileURLToPath } from "node:url";
import { transformSkill } from "../lib/github-pm/transform.mjs";

const ROOT = path.join(path.dirname(fileURLToPath(import.meta.url)), "..");
const SOURCE_FILE = path.join(ROOT, "scripts", "github-project-source.json");
const OUT_DIR = path.join(ROOT, "templates", "github-pm-ext", "github-project");
const MANIFEST_FILE = path.join(ROOT, "templates", "github-pm-ext", "MANIFEST.json");

const source = JSON.parse(await fs.readFile(SOURCE_FILE, "utf8"));
const { repo, commit, path: srcPath, license } = source;

async function fetchText(url) {
  const res = await fetch(url);
  if (!res.ok) throw new Error(`HTTP ${res.status} fetching ${url}`);
  return res.text();
}

async function listFiles(apiPath) {
  const url = `https://api.github.com/repos/${repo}/contents/${apiPath}?ref=${commit}`;
  const res = await fetch(url, { headers: { Accept: "application/vnd.github.v3+json" } });
  if (!res.ok) throw new Error(`HTTP ${res.status} listing ${url}`);
  const data = await res.json();
  return Array.isArray(data) ? data : [];
}

await fs.rm(OUT_DIR, { recursive: true, force: true });
await fs.mkdir(OUT_DIR, { recursive: true });

const entries = await listFiles(srcPath === "." ? "" : srcPath);
for (const entry of entries) {
  if (entry.type === "file" && entry.name.endsWith(".md")) {
    const raw = await fetchText(entry.download_url);
    const transformed = transformSkill(raw, { repo, commit, filePath: entry.path, license });
    await fs.writeFile(path.join(OUT_DIR, entry.name), transformed, "utf8");
    console.log(`  ✓ ${entry.name}`);
  }
}

let manifest = {};
try { manifest = JSON.parse(await fs.readFile(MANIFEST_FILE, "utf8")); } catch {}
manifest["github-project"] = { repo, commit, license, path: srcPath };
await fs.writeFile(MANIFEST_FILE, JSON.stringify(manifest, null, 2) + "\n", "utf8");

console.log("sync:github-project done");
```

- [ ] **Step 8: Create placeholder MANIFEST.json**

```bash
mkdir -p templates/github-pm-ext
```

`templates/github-pm-ext/MANIFEST.json`:
```json
{
  "_readme": "Populated by npm run sync:github-issues and npm run sync:github-project"
}
```

- [ ] **Step 9: Add scripts to `package.json`**

Read `package.json` first. Add inside the `"scripts"` object:
```json
"sync:github-issues": "node scripts/sync-github-issues.mjs",
"sync:github-project": "node scripts/sync-github-project.mjs"
```

- [ ] **Step 10: Create `lib/github-pm/` directory marker**

```bash
mkdir -p lib/github-pm
```

- [ ] **Step 11: Run vendoring**

```bash
npm run sync:github-issues
npm run sync:github-project
```

Expected: files appear in `templates/github-pm-ext/github-issues/` and `templates/github-pm-ext/github-project/`.

- [ ] **Step 12: Run npm test**

```bash
npm test
```

Expected: pass (new sync scripts not in lint/typecheck scope if in scripts/, but `lib/github-pm/transform.mjs` is — verify no errors)

- [ ] **Step 13: Commit**

```bash
git add scripts/github-issues-source.json scripts/github-project-source.json \
        scripts/sync-github-issues.mjs scripts/sync-github-project.mjs \
        lib/github-pm/transform.mjs \
        templates/github-pm-ext/ \
        package.json
git commit -m "feat: add vendoring infrastructure for github-issues and github-project skills"
```

---

## Task 8: Catalog + buildPlan integration (TDD)

**Files:**
- Create: `tests/catalog-github-pm.test.mjs`
- Create: `tests/plan-github-pm.test.mjs`
- Create: `lib/data/github-pm-catalog.mjs`
- Modify: `lib/data/asset-catalog.mjs`
- Modify: `lib/plan.mjs`

**Interfaces:**
- Consumes: `detectGitHubPM` → `profile.githubPM` (Task 1)
- Consumes: `tests/fixtures/github-project/` (Task 1)
- Produces: `selectGitHubPMAssets(profile) → GitHubPMArtifactDef[]`
- Produces: `buildPlan` includes `category:'github-pm'` artifacts

- [ ] **Step 1: Write failing catalog test**

```js
// tests/catalog-github-pm.test.mjs
import { test } from "node:test";
import assert from "node:assert/strict";
import { selectGitHubPMAssets } from "../lib/data/github-pm-catalog.mjs";

function profile(p) {
  return { root: "/x", primaryLanguage: "TypeScript", frameworks: [], vcs: { isGit: true, remoteUrl: null, defaultBranch: "main", worktreeReady: true }, ...p };
}

test("returns [] when githubPM not detected", () => {
  const a = selectGitHubPMAssets(profile({ githubPM: { detected: false, hasIssueTemplates: false, hasWorkflows: false, hasPmConfig: false } }));
  assert.deepEqual(a, []);
});

test("returns [] when githubPM absent", () => {
  assert.deepEqual(selectGitHubPMAssets(profile({})), []);
});

test("returns artifacts when detected", () => {
  const a = selectGitHubPMAssets(profile({ githubPM: { detected: true, hasIssueTemplates: false, hasWorkflows: false, hasPmConfig: false } }));
  assert.ok(a.length > 0);
});

test("every artifact has id, dest, copyFrom, skipIfExists", () => {
  const a = selectGitHubPMAssets(profile({ githubPM: { detected: true, hasIssueTemplates: false, hasWorkflows: false, hasPmConfig: false } }));
  for (const item of a) {
    assert.ok(item.id, `id missing`);
    assert.ok(item.dest, `dest missing on ${item.id}`);
    assert.ok(item.copyFrom, `copyFrom missing on ${item.id}`);
    assert.equal(typeof item.skipIfExists, "boolean", `skipIfExists not boolean on ${item.id}`);
  }
});

test("github-pm:skill artifact present", () => {
  const a = selectGitHubPMAssets(profile({ githubPM: { detected: true, hasIssueTemplates: false, hasWorkflows: false, hasPmConfig: false } }));
  assert.ok(a.some(x => x.id === "github-pm:skill"));
});

test("github-pm:pm-config has skipIfExists:true", () => {
  const a = selectGitHubPMAssets(profile({ githubPM: { detected: true, hasIssueTemplates: false, hasWorkflows: false, hasPmConfig: false } }));
  const cfg = a.find(x => x.id === "github-pm:pm-config");
  assert.ok(cfg, "pm-config artifact missing");
  assert.equal(cfg.skipIfExists, true);
});

test("IDs are unique", () => {
  const a = selectGitHubPMAssets(profile({ githubPM: { detected: true, hasIssueTemplates: false, hasWorkflows: false, hasPmConfig: false } }));
  const ids = a.map(x => x.id);
  assert.deepEqual(ids, [...new Set(ids)]);
});
```

- [ ] **Step 2: Write failing plan test**

```js
// tests/plan-github-pm.test.mjs
import { test } from "node:test";
import assert from "node:assert/strict";
import path from "node:path";
import { fileURLToPath } from "node:url";
import { scanProject } from "../lib/detect/index.mjs";
import { buildPlan } from "../lib/plan.mjs";

const ROOT = path.join(path.dirname(fileURLToPath(import.meta.url)), "..");
const FIX = path.join(path.dirname(fileURLToPath(import.meta.url)), "fixtures");

test("no github-pm artifacts when not a github project", () => {
  const profile = scanProject(path.join(FIX, "js-ts-app"));
  const plan = buildPlan(profile, { pluginRoot: ROOT });
  assert.equal(plan.artifacts.filter(a => a.category === "github-pm").length, 0);
});

test("github-pm artifacts present when detected", () => {
  const profile = scanProject(path.join(FIX, "github-project"));
  const plan = buildPlan(profile, { pluginRoot: ROOT });
  const ghPm = plan.artifacts.filter(a => a.category === "github-pm");
  assert.ok(ghPm.length > 0, "no github-pm artifacts in plan");
});

test("all github-pm artifacts have defaultSelected:false", () => {
  const profile = scanProject(path.join(FIX, "github-project"));
  const plan = buildPlan(profile, { pluginRoot: ROOT });
  for (const a of plan.artifacts.filter(x => x.category === "github-pm")) {
    assert.equal(a.defaultSelected, false, `${a.id} should be opt-in`);
  }
});

test("all github-pm artifacts have contextCost:0", () => {
  const profile = scanProject(path.join(FIX, "github-project"));
  const plan = buildPlan(profile, { pluginRoot: ROOT });
  for (const a of plan.artifacts.filter(x => x.category === "github-pm")) {
    assert.equal(a.contextCost, 0, `${a.id} should be lazy`);
  }
});

test("github-pm:skill artifact has correct relPath", () => {
  const profile = scanProject(path.join(FIX, "github-project"));
  const plan = buildPlan(profile, { pluginRoot: ROOT });
  const skill = plan.artifacts.find(a => a.id === "github-pm:skill");
  assert.ok(skill, "github-pm:skill missing from plan");
  assert.equal(skill.relPath, ".claude/skills/github-pm");
});

test("github-pm:pm-config artifact has correct relPath", () => {
  const profile = scanProject(path.join(FIX, "github-project"));
  const plan = buildPlan(profile, { pluginRoot: ROOT });
  const cfg = plan.artifacts.find(a => a.id === "github-pm:pm-config");
  assert.ok(cfg, "github-pm:pm-config missing from plan");
  assert.equal(cfg.relPath, ".claude/pm-config.json");
});
```

- [ ] **Step 3: Run tests — expect FAIL**

```bash
node --test tests/catalog-github-pm.test.mjs tests/plan-github-pm.test.mjs
```

Expected: `Cannot find module '../lib/data/github-pm-catalog.mjs'`

- [ ] **Step 4: Create `lib/data/github-pm-catalog.mjs`**

```js
/**
 * GitHub PM distributable-asset catalog.
 * Parallel to ecc-catalog.mjs — one module per provenance.
 *
 * Templates live in:
 *   templates/skills/github-pm/      first-party skill
 *   templates/commands/pm/           10 /pm:* commands
 *   templates/github/                ISSUE_TEMPLATE, workflows, pm-config
 *   templates/github-pm-ext/         vendored github-issues + github-project
 *
 * @module data/github-pm-catalog
 */
import path from "node:path";
import { fileURLToPath } from "node:url";

const PLUGIN_ROOT = path.join(path.dirname(fileURLToPath(import.meta.url)), "..", "..");
const T = path.join(PLUGIN_ROOT, "templates");

/**
 * @typedef {Object} GitHubPMArtifactDef
 * @property {string} id
 * @property {string} description
 * @property {string} copyFrom   Absolute path in templates/.
 * @property {string} dest       Relative path in target project.
 * @property {boolean} skipIfExists
 */

/** @type {GitHubPMArtifactDef[]} */
const GITHUB_PM_ARTIFACTS = [
  {
    id: "github-pm:skill",
    description: "First-party GitHub PM skill (lifecycle orchestration + delegation map)",
    copyFrom: path.join(T, "skills", "github-pm"),
    dest: ".claude/skills/github-pm",
    skipIfExists: false,
  },
  {
    id: "github-pm:commands",
    description: "10 /pm:* commands for the full issue→worktree→PR→merge loop",
    copyFrom: path.join(T, "commands", "pm"),
    dest: ".claude/commands/pm",
    skipIfExists: false,
  },
  {
    id: "github-pm:issue-templates",
    description: "GitHub issue templates: bug, feature, task",
    copyFrom: path.join(T, "github", "ISSUE_TEMPLATE"),
    dest: ".github/ISSUE_TEMPLATE",
    skipIfExists: false,
  },
  {
    id: "github-pm:pr-template",
    description: "GitHub pull request template with acceptance criteria checklist",
    copyFrom: path.join(T, "github", "PULL_REQUEST_TEMPLATE.md"),
    dest: ".github/PULL_REQUEST_TEMPLATE.md",
    skipIfExists: false,
  },
  {
    id: "github-pm:workflows",
    description: "4 GitHub Actions workflows: issue lifecycle → Projects v2 status automation",
    copyFrom: path.join(T, "github", "workflows"),
    dest: ".github/workflows",
    skipIfExists: false,
  },
  {
    id: "github-pm:pm-config",
    description: "pm-config.json template — fill real IDs via /pm:setup-project (never overwritten)",
    copyFrom: path.join(T, "github", "pm-config.json.template"),
    dest: ".claude/pm-config.json",
    skipIfExists: true,
  },
  {
    id: "github-pm:github-issues",
    description: "Vendored skill: github-issues (github/awesome-copilot, MIT)",
    copyFrom: path.join(T, "github-pm-ext", "github-issues"),
    dest: ".claude/skills/github-issues",
    skipIfExists: false,
  },
  {
    id: "github-pm:github-project",
    description: "Vendored skill: github-project (netresearch/github-project-skill, MIT AND CC-BY-SA-4.0)",
    copyFrom: path.join(T, "github-pm-ext", "github-project"),
    dest: ".claude/skills/github-project",
    skipIfExists: false,
  },
];

/**
 * Select GitHub PM artifacts for a profile. Returns [] if not detected.
 * @param {import('../profile.mjs').ProjectProfile} profile
 * @returns {GitHubPMArtifactDef[]}
 */
export function selectGitHubPMAssets(profile) {
  if (!profile.githubPM?.detected) return [];
  return GITHUB_PM_ARTIFACTS;
}
```

- [ ] **Step 5: Add re-export to `lib/data/asset-catalog.mjs`**

Add after the tools-catalog re-export block:

```js
// GitHub PM (first-party + vendored) — templates/skills/github-pm/ + templates/github/ + templates/github-pm-ext/
export { selectGitHubPMAssets } from "./github-pm-catalog.mjs";
```

- [ ] **Step 6: Integrate into `lib/plan.mjs`**

Add import at top of plan.mjs, after existing imports from `./data/asset-catalog.mjs`:
```js
import { selectGitHubPMAssets } from "./data/asset-catalog.mjs";
```

(Note: it's already imported via the barrel if you add it to the barrel import line, or add as a separate import. Check if the barrel already imports everything in one line, and add `selectGitHubPMAssets` to that destructuring.)

Add the github-pm block in `buildPlan`, after the existing `addToolArtifacts` call and before the `// --- Notes ---` section:

```js
  // --- GitHub PM (opt-in; defaultSelected:false) ---
  for (const asset of selectGitHubPMAssets(profile)) {
    add({
      id: asset.id,
      relPath: asset.dest,
      title: asset.description,
      category: /** @type {any} */ ("github-pm"),
      rationale: asset.description,
      contextCost: 0,
      defaultSelected: false,
      copyFrom: asset.copyFrom,
    });
  }
```

Also update the `Artifact` typedef in plan.mjs to include `"github-pm"` in the category union:
```js
 * @property {"claude-md"|"rules"|"settings"|"mcp"|"hooks"|"skills"|"agents"|"commands"|"tools"|"worktree"|"lsp"|"docs"|"script"|"github-pm"} category
```

- [ ] **Step 7: Run tests — expect PASS**

```bash
node --test tests/catalog-github-pm.test.mjs tests/plan-github-pm.test.mjs
```

Expected: all tests pass

- [ ] **Step 8: Run full test suite**

```bash
npm test
```

Expected: typecheck + lint + all unit tests pass

- [ ] **Step 9: Commit**

```bash
git add lib/data/github-pm-catalog.mjs lib/data/asset-catalog.mjs lib/plan.mjs \
        tests/catalog-github-pm.test.mjs tests/plan-github-pm.test.mjs
git commit -m "feat: add github-pm-catalog + buildPlan integration (category github-pm, opt-in)"
```

---

## Task 9: Plugin commands — `/add-github-pm` + existing command updates

**Files:**
- Create: `commands/add-github-pm.md`
- Modify: `commands/init.md`
- Modify: `commands/patch.md`
- Modify: `commands/scan.md`
- Modify: `commands/doctor.md`

**Before starting:** Read all four existing files in full to understand current content precisely. Changes are surgical insertions, not rewrites.

- [ ] **Step 1: Read existing commands**

```bash
cat commands/init.md
cat commands/patch.md
cat commands/scan.md
cat commands/doctor.md
```

- [ ] **Step 2: Create `commands/add-github-pm.md`**

```markdown
---
description: Activate the GitHub PM pillar in a project that already has the harness configured
argument-hint: "[path]"
allowed-tools:
  - Bash
  - AskUserQuestion
---

# Add GitHub PM to an existing harness

Target directory: `$1` if provided, else `$CLAUDE_PROJECT_DIR`.

Activate the GitHub PM pillar (issues, Projects v2, commands, workflows) in a project
that already has the harness configured. Analogous to `/add-mcp` and `/add-tools`.

## Flow

1. **Scan** the project to check detection:

   ```bash
   "${CLAUDE_PLUGIN_ROOT}/bin/aia-harness" scan "${1:-$CLAUDE_PROJECT_DIR}" --json
   ```

   Parse `profile.githubPM.detected`. If `false`:
   → "Remote URL is not github.com — GitHub PM is not applicable to this project." Encerrar.

2. **Plan** (GitHub PM category only):

   ```bash
   "${CLAUDE_PLUGIN_ROOT}/bin/aia-harness" plan "${1:-$CLAUDE_PROJECT_DIR}" --json
   ```

   Filter artifacts with `category === 'github-pm'`. Show the list with rationale.

3. **Confirm** with `AskUserQuestion`:
   "Install GitHub PM artifacts? (skill, 10 commands, issue templates, 4 workflows, pm-config template)"
   Options: "Yes, install" / "No, cancel"

4. **Dry run preview** then **apply**:

   ```bash
   "${CLAUDE_PLUGIN_ROOT}/bin/aia-harness" apply "${1:-$CLAUDE_PROJECT_DIR}" --yes --only=github-pm
   ```

5. **Post-install instructions:**
   "GitHub PM instalado. Próximo passo: rode `/pm:setup-project` para configurar
   o Project ID e os status IDs do Projects v2."
```

- [ ] **Step 3: Update `commands/scan.md`**

Add a step after step 3 (current last step) to include `githubPM` in the diagnosis:

```markdown
4. If `profile.githubPM.detected` is true, add a "GitHub PM" section to the report:
   - Remote: github.com detected ✓
   - Issue templates: present / absent
   - Workflows: present / absent
   - pm-config.json: configured / not configured
   - Suggest `/add-github-pm` if not yet installed.
```

- [ ] **Step 4: Update `commands/patch.md`**

In the "Category label / ID prefix" table, add a row:

```markdown
| `github-pm` — GitHub PM skill, commands, templates, workflows | `category === 'github-pm'` |
```

Note: the patch command matches by `id prefix` currently. Since github-pm artifacts use `id` starting with `github-pm:`, the match condition is `starts with 'github-pm:'`.

Find the existing table in patch.md and add after the last row:
```markdown
| `github-pm` — skill, commands, templates, workflows | starts with `github-pm:` |
```

- [ ] **Step 5: Update `commands/init.md`**

In the consent gate step (Step 3), add `github-pm` as a group in the `AskUserQuestion`:

Find the section describing `AskUserQuestion` groups. After mentioning `tools`:
```markdown
   **If `profile.githubPM.detected` is true**, also offer a dedicated single-select
   question: "GitHub PM (issues, Projects v2, workflows)".
   Phrase: _"O projeto usa GitHub. Instalar o pillar GitHub PM? (skill /pm:*, issue templates,
   4 GitHub Actions workflows, pm-config template)"_
   Options: **"Sim, instalar"** (→ include `github-pm` category) first, **"Não"** second.
   Mention: "Requer configuração via `/pm:setup-project` após a instalação."
   `defaultSelected: false` — always opt-in.
```

- [ ] **Step 6: Update `commands/doctor.md`**

Read doctor.md completely. Find where it audits artifact categories. Add github-pm audit:

Add a check block for github-pm. Pattern from existing doctor checks:
```markdown
### GitHub PM

If `profile.githubPM.detected`:
- Check: `.claude/skills/github-pm/SKILL.md` exists
- Check: `.claude/commands/pm/` directory has 10 command files
- Check: `.github/ISSUE_TEMPLATE/` has bug.yml, feature.yml, task.yml
- Check: `.github/workflows/` has issue-to-project.yml, commit-to-progress.yml,
  pr-to-review.yml, auto-close-issue.yml
- Check: `.claude/pm-config.json` exists (warn if still has REPLACE_ME placeholders)
- Check: `.claude/skills/github-issues/` exists (vendored)
- Check: `.claude/skills/github-project/` exists (vendored)

If any check fails → report as missing with `apply --only=github-pm` as fix suggestion.
If `profile.githubPM.detected` is false → skip section silently.
```

- [ ] **Step 7: Run npm test**

```bash
npm test
```

Expected: pass

- [ ] **Step 8: Commit**

```bash
git add commands/add-github-pm.md commands/init.md commands/patch.md commands/scan.md commands/doctor.md
git commit -m "feat: add /add-github-pm command + integrate github-pm into init/patch/scan/doctor"
```

---

## Task 10: Final validation

- [ ] **Step 1: Run full test suite**

```bash
npm test
```

Expected output (all three lines pass):
```
✓ typecheck (tsc --noEmit)
✓ lint (eslint)
✓ unit tests (node --test)
```

- [ ] **Step 2: Smoke test detector with a real project (if available)**

```bash
node bin/harness.mjs scan . --json | python3 -c "import sys,json; d=json.load(sys.stdin); print(d.get('githubPM', 'MISSING'))"
```

Expected: `{'detected': True/False, 'hasIssueTemplates': ..., 'hasWorkflows': ..., 'hasPmConfig': ...}`

- [ ] **Step 3: Smoke test plan**

```bash
node bin/harness.mjs plan . --json | python3 -c "
import sys,json
d=json.load(sys.stdin)
ghpm=[a for a in d['artifacts'] if a['category']=='github-pm']
print(f'{len(ghpm)} github-pm artifacts')
for a in ghpm: print(f'  {a[\"id\"]} | defaultSelected={a[\"defaultSelected\"]}')
"
```

Expected (on aia-harness repo itself, which has github.com remote):
```
8 github-pm artifacts
  github-pm:skill | defaultSelected=False
  github-pm:commands | defaultSelected=False
  ...
```

- [ ] **Step 4: Verify typecheck catches `'github-pm'` category**

The `Artifact` typedef union must include `"github-pm"`. Run:
```bash
npm run typecheck
```

Expected: no errors.

- [ ] **Step 5: Final commit**

```bash
git add -p  # review any remaining unstaged changes
npm test    # last confirmation
git commit -m "feat: GitHub PM pillar complete — scan/plan/apply + 10 commands + workflows" \
  --allow-empty-message
```

---

## Self-Review

**Spec coverage:**

| Spec section | Task | Status |
| --- | --- | --- |
| 1. Detection (`lib/detect/github-pm.mjs`) | Task 1 | ✓ |
| 2. Catalog (`lib/data/github-pm-catalog.mjs`) | Task 8 | ✓ |
| 3. Templates structure | Tasks 3-6 | ✓ |
| 4. Skill first-party SKILL.md + references | Task 3 | ✓ |
| 5. GitHub Actions workflows | Task 6 | ✓ |
| 6. Command patterns | Tasks 4-5 | ✓ |
| 7.1 `/pm:worktree-new` | Task 5 | ✓ |
| 7.2 `/pm:commit-push-pr` | Task 5 | ✓ |
| 7.3 `/pm:pr-merge` | Task 5 | ✓ |
| 7.4 `/pm:worktree-remove` | Task 5 | ✓ |
| 7.5 `/pm:code-review-pr` | Task 5 | ✓ |
| 8. Shell scripts | Task 2 | ✓ |
| 9. `/pm:setup-project` | Task 4 | ✓ |
| 10. Vendoring + sync scripts | Task 7 | ✓ |
| 11. `/add-github-pm` | Task 9 | ✓ |
| 12. Integration (`/init`, `/doctor`, `/patch`, `/scan`) | Task 9 | ✓ |
| 13. Tests | Tasks 1, 8 | ✓ |
| 16. `npm test` | Task 10 | ✓ |
| VcsInfo.remoteUrl (needed by detector) | Task 1 | ✓ (spec omits — fixed) |
| `files[].rel` not `files[].includes` (spec bug) | Task 1 | ✓ (fixed in detector) |

**Placeholder scan:** No TBD, TODO, or incomplete steps. All code blocks complete.

**Type consistency:**
- `GitHubPMInfo` defined in Task 1 → used by `detectGitHubPM` (Task 1) → catalog check (Task 8)
- `GitHubPMArtifactDef` defined in Task 8 → consumed by `buildPlan` (Task 8)
- `profile.vcs.remoteUrl` added to typedef + implementation in Task 1
- `category: "github-pm"` string added to `Artifact` typedef union in Task 8
